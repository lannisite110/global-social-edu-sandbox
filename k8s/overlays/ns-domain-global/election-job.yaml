apiVersion: batch/v1
kind: Job
metadata:
  name: election-hash-demo
  labels:
    edu.web3/plugin-id: edu.global.sandbox.election
    edu.web3/compliance: fabric-sandbox-only
    edu.web3/namespace: ns-domain-global
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        edu.web3/plugin-id: edu.global.sandbox.election
    spec:
      restartPolicy: Never
      containers:
        - name: fabric-runner
          image: hyperledger/fabric-tools:2.5
          env:
            - name: CHAIN_ID
              value: fabric-local
            - name: FIXTURE_PATH
              value: fixtures/election-nodes-sample.json
            - name: TASK_TYPE
              value: GLOBAL_ELECTION_HASH_DEMO
          command:
            - sh
            - -ec
            - |
              echo "==> ${TASK_TYPE} fabric-local gate"
              peer version | head -1
              case "${FIXTURE_PATH}" in
                fixtures/*.json) echo "fixture OK: ${FIXTURE_PATH}" ;;
                *) exit 1 ;;
              esac
              echo "election hash demo gate OK — fictional jurisdiction only"
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 256Mi }
